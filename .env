# ==========================================
# HEALIT MOBILE APP — ENVIRONMENT CONFIG
# ==========================================

# Base API Server Endpoint (Change to your hosted server URL when deployed)
EXPO_PUBLIC_API_URL=https://api.healit.app/api/v1

# Environment Mode: 'development' | 'staging' | 'production'
EXPO_PUBLIC_ENV=production

# Graceful Mock Data Fallback (True enables instant local demo when backend is offline)
EXPO_PUBLIC_ENABLE_MOCK_FALLBACK=true

# API Request Timeout in Milliseconds
EXPO_PUBLIC_API_TIMEOUT=15000
